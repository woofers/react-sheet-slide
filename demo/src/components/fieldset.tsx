import { styled } from 'stitches'

export const Legend = styled('legend', {
  float: 'left',
  userSelect: 'none',
  display: 'inline-flex',
  alignItems: 'center'
})

export const Fieldset = styled('fieldset', {
  border: 0,
  padding: 0,
  display: 'flex',
  gap: '0 8px'
})
